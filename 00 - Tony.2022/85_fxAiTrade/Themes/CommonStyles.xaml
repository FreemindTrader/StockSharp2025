<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
    xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui"
    xmlns:dxwuii="http://schemas.devexpress.com/winfx/2008/xaml/windowsui/internal" xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors">
    <SolidColorBrush x:Key="appBarBackgroundBrush" Color="#3f3f3f" />
    <Style x:Key="AppBarItemBaseStyle" TargetType="Control">
        <Setter Property="HorizontalAlignment" Value="Center"/>
    </Style>
    <Style x:Key="AppBarButtonBaseStyle" TargetType="dxwui:AppBarButton" BasedOn="{StaticResource AppBarItemBaseStyle}">
        <Setter Property="IsEllipseEnabled" Value="False"/>
        <Setter Property="AllowGlyphTheming" Value="True" />
        <Setter Property="GlyphStretch" Value="UniformToFill" />
    </Style>
    <Style TargetType="dxwui:AppBar">
        <Setter Property="Background" Value="{StaticResource appBarBackgroundBrush}"/>
    </Style>
    <Style TargetType="dxwui:AppBarButton" BasedOn="{StaticResource AppBarButtonBaseStyle}"/>
    <Style TargetType="dxwui:AppBarSeparator" BasedOn="{StaticResource AppBarItemBaseStyle}"/>
    <Style TargetType="dxwui:AppBarToggleButton" BasedOn="{StaticResource AppBarButtonBaseStyle}"/>
    
    <Style x:Key="AmountTile" TargetType="{x:Type dxlc:Tile}">
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="Size" Value="ExtraSmall" />
        <Setter Property="HeaderTemplate">
            <Setter.Value>
                <DataTemplate />
            </Setter.Value>
        </Setter>
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <StackPanel>
                        <dxe:ImageEdit
                                EditValue="{Binding Mode=OneWay}"
                                IsReadOnly="True"
                                ShowBorder="False"
                                Stretch="UniformToFill" />
                        <TextBlock
                                Margin="0,5,0,0"
                                HorizontalAlignment="Center"
                                FontSize="14"
                                Text="{Binding Path=Header, RelativeSource={RelativeSource AncestorType=dxlc:Tile}}" />
                    </StackPanel>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <Style TargetType="ContentControl">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="IsTabStop" Value="False" />
    </Style>
    <Style x:Key="EmptyButton" TargetType="{x:Type Button}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <ContentPresenter Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="dxwui:PageAdornerControl">
        <Setter Property="Padding" Value="0,-46,0,0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="dxwui:PageAdornerControl">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid Margin="42,0,32,42">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Grid>
                                <dxwuii:NavigationHeaderControl
                                    x:Name="PART_NavigationHeader"
                                    Margin="0,8,0,17"
                                    VerticalAlignment="Center"
                                    BackCommand="{TemplateBinding BackCommand}"
                                    Content="{TemplateBinding Header}"
                                    ContentTemplate="{TemplateBinding HeaderTemplate}"
                                    ShowBackButton="{TemplateBinding ShowBackButton}" />
                            </Grid>
                            <ContentPresenter Grid.Row="1" Margin="{TemplateBinding Padding}" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="HeaderTemplate">
            <Setter.Value>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock
                            Margin="10,0,0,0"
                            FontFamily="Segoe UI Light, Tahoma Light"
                            FontSize="20"
                            Foreground="#FF2F51A5"
                            IsHitTestVisible="False"
                            Text="{Binding}" />
                    </StackPanel>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style BasedOn="{StaticResource EmptyButton}" TargetType="dxwuii:BackButton">
        <Setter Property="Margin" Value="-32,3,1,-3" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Image Source="/TradeStation.Xpf;component/Images/Back.png" Stretch="None" />
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="ModuleLinkButton" TargetType="Button">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid Background="#00FF0000" Cursor="Hand">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualStateGroup.Transitions>
                                    <VisualTransition GeneratedDuration="0" To="MouseOver">
                                        <Storyboard>
                                            <DoubleAnimation
                                                Duration="0:0:0.15"
                                                Storyboard.TargetName="TextBlock"
                                                Storyboard.TargetProperty="(UIElement.Opacity)"
                                                To="1" />
                                        </Storyboard>
                                    </VisualTransition>
                                    <VisualTransition From="MouseOver" GeneratedDuration="0">
                                        <Storyboard>
                                            <DoubleAnimation
                                                Duration="0:0:0.3"
                                                Storyboard.TargetName="TextBlock"
                                                Storyboard.TargetProperty="(UIElement.Opacity)"
                                                To="0.5" />
                                        </Storyboard>
                                    </VisualTransition>
                                    <VisualTransition From="Disabled" GeneratedDuration="0">
                                        <Storyboard>
                                            <DoubleAnimation
                                                Duration="0:0:0.2"
                                                Storyboard.TargetName="TextBlock"
                                                Storyboard.TargetProperty="(UIElement.Opacity)"
                                                To="0.5" />
                                        </Storyboard>
                                    </VisualTransition>
                                </VisualStateGroup.Transitions>
                                <VisualState x:Name="Disabled">
                                    <Storyboard>
                                        <DoubleAnimation
                                            Duration="0:0:0.5"
                                            Storyboard.TargetName="TextBlock"
                                            Storyboard.TargetProperty="(UIElement.Opacity)"
                                            To="1.0" />
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="TextBlock" Storyboard.TargetProperty="Foreground">
                                            <DiscreteObjectKeyFrame KeyTime="0">
                                                <DiscreteObjectKeyFrame.Value>
                                                    <SolidColorBrush>#f78a09</SolidColorBrush>
                                                </DiscreteObjectKeyFrame.Value>
                                            </DiscreteObjectKeyFrame>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="MouseOver">
                                    <Storyboard>
                                        <DoubleAnimation
                                            Duration="0:0:0.15"
                                            Storyboard.TargetName="TextBlock"
                                            Storyboard.TargetProperty="(UIElement.Opacity)"
                                            To="1.0" />
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed" />
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <TextBlock
                            x:Name="TextBlock"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            FontSize="18"
                            Foreground="#FFE3E3E3"
                            Opacity="0.5"
                            Text="{TemplateBinding Content}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>