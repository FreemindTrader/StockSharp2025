<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:li1="clr-namespace:StockSharp.Xaml.Charting.CustomAnnotations"
    xmlns:sysXaml="clr-namespace:System.Windows.Markup;assembly=System.Xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/SciChart.Charting;component/Themes/Default.xaml" />
    </ResourceDictionary.MergedDictionaries>
    <Style BasedOn="{StaticResource AnnotationBaseStyle}" TargetType="{x:Type li1:RulerAnnotation}">
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type li1:RulerAnnotation}">
                    <Grid x:Name="PART_AnnotationRoot">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid
                            x:Name="PART_RulerArea"
                            Width="{TemplateBinding li1:RulerAnnotation.RulerWidth}"
                            Height="{TemplateBinding li1:RulerAnnotation.RulerHeight}"
                            HorizontalAlignment="Center"
                            Background="{TemplateBinding Control.Background}"
                            Row="0">
                            <Line
                                VerticalAlignment="Center"
                                Stretch="Fill"
                                Stroke="DimGray"
                                StrokeDashArray="2,3"
                                StrokeThickness="1"
                                X1="0"
                                X2="1"
                                Y1="0"
                                Y2="0" />
                            <Line
                                HorizontalAlignment="Center"
                                Stretch="Fill"
                                Stroke="DimGray"
                                StrokeDashArray="2,3"
                                StrokeThickness="1"
                                X1="0"
                                X2="0"
                                Y1="0"
                                Y2="1" />
                        </Grid>
                        <Border
                            x:Name="PART_TextBorder"
                            Grid.Row="1"
                            Margin="0,5,0,0"
                            HorizontalAlignment="Center"
                            Background="#E05F9EA0"
                            BorderThickness="0"
                            CornerRadius="3"
                            Padding="5,7">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <FrameworkElement.Resources>
                                    <Style TargetType="{x:Type TextBlock}">
                                        <Setter Property="TextBlock.Foreground" Value="White" />
                                        <Setter Property="TextBlock.FontSize" Value="14" />
                                        <Setter Property="TextBlock.FontWeight" Value="SemiBold" />
                                        <Setter Property="FrameworkElement.VerticalAlignment" Value="Center" />
                                    </Style>
                                </FrameworkElement.Resources>
                                <TextBlock Grid.Row="0" Text="{TemplateBinding li1:RulerAnnotation.Text1}" />
                                <TextBlock Grid.Row="1" Text="{TemplateBinding li1:RulerAnnotation.Text2}" />
                            </Grid>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <DrawingBrush
        x:Key="GridBrush"
        Stretch="None"
        TileMode="Tile"
        Viewport="0,0 8,5"
        ViewportUnits="Absolute">
        <DrawingBrush.Drawing>
            <DrawingGroup>
                <GeometryDrawing Brush="{Binding Background}">
                    <GeometryDrawing.Geometry>
                        <RectangleGeometry Rect="0,0,8,5" />
                    </GeometryDrawing.Geometry>
                </GeometryDrawing>
                <GeometryDrawing>
                    <GeometryDrawing.Geometry>
                        <GeometryGroup>
                            <LineGeometry StartPoint="0,0" EndPoint="8,5" />
                            <LineGeometry StartPoint="0,5" EndPoint="8,0" />
                        </GeometryGroup>
                    </GeometryDrawing.Geometry>
                    <GeometryDrawing.Pen>
                        <Pen Brush="{Binding Foreground}" Thickness="1" />
                    </GeometryDrawing.Pen>
                </GeometryDrawing>
            </DrawingGroup>
        </DrawingBrush.Drawing>
    </DrawingBrush>
</ResourceDictionary>