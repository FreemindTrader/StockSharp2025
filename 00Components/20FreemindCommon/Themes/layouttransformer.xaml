<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:layoutToolkit="clr-namespace:System.Windows.Controls">
  <Style TargetType="{x:Type layoutToolkit:LayoutTransformer}">
    <Setter Property="Control.Foreground" Value="#FF000000"/>
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type layoutToolkit:LayoutTransformer}">
          <Grid x:Name="TransformRoot" Background="{TemplateBinding Control.Background}">
            <ContentPresenter x:Name="Presenter" Content="{TemplateBinding ContentControl.Content}"
                              ContentTemplate="{TemplateBinding ContentControl.ContentTemplate}"/>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>