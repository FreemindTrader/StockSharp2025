<s:dje_z3N8YA3X6H3LN37R7S3NYSZ5TJDF5WRBWWD5KML6BFKXZ44S823J6BQY3A87QVC3HBBYCP4RA_ejd
    x:Name="Ctrl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:dx="clr-namespace:DevExpress.Xpf.Core;assembly=DevExpress.Xpf.Core.v22.1"
    xmlns:l1i="clr-namespace:DevExpress.Xpf.Utils.Themes;assembly=DevExpress.Xpf.Core.v22.1"
    xmlns:l1li="clr-namespace:StockSharp.Xaml.Charting.Ultrachart"
    xmlns:local="clr-namespace:StockSharp.Xaml.Charting"
    xmlns:l1lll="clr-namespace:DevExpress.Xpf.Editors.Themes;assembly=DevExpress.Xpf.Core.v22.1"
    xmlns:ll1="clr-namespace:Ecng.Xaml.Converters;assembly=StockSharp.Xaml"
    xmlns:loc="clr-namespace:StockSharp.Xaml;assembly=StockSharp.Xaml"
    xmlns:s="clr-namespace:-"
    xmlns:sx="http://schemas.stocksharp.com/xaml"
    HorizontalAlignment="Left"
    VerticalAlignment="Top"
    BorderBrush="Transparent"
    BorderThickness="1"
    FontSize="12"
    FontWeight="Bold"
    ItemsSource="{Binding Elements}"
    Padding="5">
    <FrameworkElement.Resources>
        <s:dje_z6N2QS39S3KT8AENPC4W473E8S2LJVDDWX5UHK38BLR3PDJQ_ejd x:Key="BooleanToVisibilityConverter" />
        <ll1:ColorToBrushConverter x:Key="ColorToBrushConverter" />
        <s:dje_zTYQED6UNCPRW4ZAGFE6CK5DWJPCS3SGKTB2MQGAM2NNWYBMEVZ992Z7LLS2PR94A8Q_ejd x:Key="UltrachartElementToSettingsObjectConverter" />
        <l1li:AreaAxesToBorderColorConverter x:Key="axesToBorderConv" />
        <l1li:AreaAxesToTooltipConverter x:Key="axesToTooltipConv" />
        <Popup
            x:Key="serPopup"
            AllowsTransparency="True"
            Focusable="True"
            HorizontalOffset="0"
            Placement="Bottom"
            PopupAnimation="Fade"
            StaysOpen="False"
            VerticalOffset="0">
            <dx:BackgroundPanel>
                <Border BorderBrush="{DynamicResource {l1lll:ColorChooserThemeKey ResourceKey=BorderBrush}}" BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <sx:CandleDataTypeEdit x:Name="candleSettingsEditor" Grid.Row="0" />
                        <dx:SimpleButton
                            Grid.Row="1"
                            Margin="0,3"
                            HorizontalAlignment="Center"
                            Content="{loc:Localization Change}"
                            FontSize="12"
                            Padding="3,1" />
                    </Grid>
                </Border>
            </dx:BackgroundPanel>
        </Popup>
    </FrameworkElement.Resources>
    <ItemsControl.ItemTemplate>
        <DataTemplate DataType="local:UltrachartCompositeElement">
            <Border>
                <Border.Background>
                    <MultiBinding Converter="{StaticResource axesToBorderConv}">
                        <Binding Path="ChartElement.XAxis" />
                        <Binding Path="ChartElement.YAxis" />
                    </MultiBinding>
                </Border.Background>
                <FrameworkElement.ToolTip>
                    <MultiBinding Converter="{StaticResource axesToTooltipConv}">
                        <Binding Path="ChartElement.XAxis" />
                        <Binding Path="ChartElement.YAxis" />
                    </MultiBinding>
                </FrameworkElement.ToolTip>
                <Grid HorizontalAlignment="Left">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <CheckBox
                        Grid.Column="0"
                        Width="16"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        IsChecked="{Binding ChartElement.IsVisible, Mode=TwoWay}"
                        Visibility="{Binding ElementName=Ctrl, Path=DataContext.AllowToHide, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    <local:ChartElementEditor
                        Grid.Column="1"
                        Width="16"
                        Height="16">
                        <FrameworkElement.DataContext>
                            <MultiBinding Converter="{StaticResource UltrachartElementToSettingsObjectConverter}">
                                <Binding Path="ChartElement" />
                                <Binding FallbackValue="{x:Null}" Path="ChartElement.IndicatorPainter" />
                            </MultiBinding>
                        </FrameworkElement.DataContext>
                    </local:ChartElementEditor>
                    <dx:SimpleButton
                        Grid.Column="2"
                        Width="10"
                        Height="10"
                        Margin="2,0,0,0"
                        Command="{Binding DataContext.RemoveElementCommand, RelativeSource={RelativeSource AncestorType=s:dje_z3N8YA3X6H3LN37R7S3NYSZ5TJDF5WRBWWD5KML6BFKXZ44S823J6BQY3A87QVC3HBBYCP4RA_ejd}}"
                        CommandParameter="{Binding}"
                        Visibility="{Binding AllowToRemove, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <FrameworkElement.Style>
                            <Style TargetType="{x:Type dx:SimpleButton}">
                                <Setter Property="Control.Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type dx:SimpleButton}">
                                            <Border Background="Transparent">
                                                <ContentPresenter
                                                    x:Name="PART_Content"
                                                    HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}"
                                                    VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}"
                                                    TextElement.Foreground="{TemplateBinding Control.Foreground}" />
                                                <FrameworkElement.Style>
                                                    <Style>
                                                        <Setter Property="Border.Background" Value="{x:Null}" />
                                                        <Setter Property="Border.BorderThickness" Value="0" />
                                                        <Style.Triggers>
                                                            <Trigger Property="UIElement.IsMouseOver" Value="True">
                                                                <Setter Property="Border.Background" Value="Black" />
                                                                <Setter Property="Border.BorderThickness" Value="1" />
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </FrameworkElement.Style>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </FrameworkElement.Style>
                        <Image Source="{loc:ThemedIcons Key=Remove2}" />
                    </dx:SimpleButton>
                    <Grid
                        Width="10"
                        Height="10"
                        Margin="5,0,5,0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        Background="{Binding Color, Converter={StaticResource ColorToBrushConverter}}"
                        Column="3" />
                    <StackPanel Grid.Column="4" Orientation="Horizontal">
                        <TextBlock Margin="5,0,0,0" VerticalAlignment="Center">
                            <Hyperlink Foreground="{Binding Foreground, ElementName=Ctrl}">
                                <Run Text="{Binding Title, Mode=OneWay}" />
                                <FrameworkContentElement.Style>
                                    <Style TargetType="{x:Type Hyperlink}">
                                        <Style.Triggers>
                                            <DataTrigger Value="False" Binding="{Binding Pane.GroupChart.IsInteracted}">
                                                <Setter Property="TextBlock.TextDecorations" Value="{x:Null}" />
                                                <Setter Property="FrameworkContentElement.Cursor" Value="Arrow" />
                                            </DataTrigger>
                                            <DataTrigger Value="False" Binding="{Binding IsCandleElement}">
                                                <Setter Property="TextBlock.TextDecorations" Value="{x:Null}" />
                                                <Setter Property="FrameworkContentElement.Cursor" Value="Arrow" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </FrameworkContentElement.Style>
                            </Hyperlink>
                        </TextBlock>
                        <ItemsControl ItemsSource="{Binding Values}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="local:UltrachartElementValue">
                                    <Grid Margin="5,0,0,0" VerticalAlignment="Center">
                                        <FrameworkElement.Style>
                                            <Style TargetType="{x:Type Grid}">
                                                <Style.Triggers>
                                                    <DataTrigger Value="" Binding="{Binding Value}">
                                                        <Setter Property="UIElement.Visibility" Value="Collapsed" />
                                                    </DataTrigger>
                                                    <DataTrigger Value="{x:Null}" Binding="{Binding Value}">
                                                        <Setter Property="UIElement.Visibility" Value="Collapsed" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </FrameworkElement.Style>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="2" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0">
                                            <FrameworkElement.Style>
                                                <Style TargetType="{x:Type TextBlock}">
                                                    <Style.Triggers>
                                                        <DataTrigger Value="" Binding="{Binding Name}">
                                                            <Setter Property="UIElement.Visibility" Value="Collapsed" />
                                                        </DataTrigger>
                                                        <DataTrigger Value="{x:Null}" Binding="{Binding Name}">
                                                            <Setter Property="UIElement.Visibility" Value="Collapsed" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </FrameworkElement.Style>
                                            <Run Text="{Binding Name}" />
                                            :
                                        </TextBlock>
                                        <TextBlock
                                            Grid.Column="2"
                                            Foreground="{Binding Color, Converter={StaticResource ColorToBrushConverter}}"
                                            Text="{Binding Value}" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Grid>
            </Border>
        </DataTemplate>
    </ItemsControl.ItemTemplate>
    <ItemsControl.ItemsPanel>
        <ItemsPanelTemplate>
            <StackPanel IsItemsHost="True" Orientation="Vertical" />
        </ItemsPanelTemplate>
    </ItemsControl.ItemsPanel>
    <Control.Template>
        <ControlTemplate TargetType="{x:Type ItemsControl}">
            <Border
                HorizontalAlignment="{TemplateBinding FrameworkElement.HorizontalAlignment}"
                VerticalAlignment="{TemplateBinding FrameworkElement.VerticalAlignment}"
                Background="{TemplateBinding Control.Background}"
                BorderBrush="{TemplateBinding Control.BorderBrush}"
                BorderThickness="{TemplateBinding Control.BorderThickness}"
                Padding="{TemplateBinding Control.Padding}">
                <ScrollViewer
                    Focusable="False"
                    HorizontalScrollBarVisibility="Hidden"
                    s:dje_zZ2MQ59H28BQX5B8FXAJNBH2MER72RBVZKWAH528H9F679PZ_ejd.IsFocusable="False"
                    VerticalScrollBarVisibility="Hidden">
                    <ItemsPresenter />
                </ScrollViewer>
            </Border>
        </ControlTemplate>
    </Control.Template>
</s:dje_z3N8YA3X6H3LN37R7S3NYSZ5TJDF5WRBWWD5KML6BFKXZ44S823J6BQY3A87QVC3HBBYCP4RA_ejd>