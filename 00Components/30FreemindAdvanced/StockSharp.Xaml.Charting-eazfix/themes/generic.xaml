<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ann="clr-namespace:StockSharp.Xaml.Charting.Ultrachart">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/StockSharp.Xaml.Charting;component/Themes/Default.xaml" />
        <ResourceDictionary Source="/StockSharp.Xaml.Charting;component/Themes/LayoutTransformer.xaml" />
    </ResourceDictionary.MergedDictionaries>
    <Style BasedOn="{StaticResource AnnotationBaseStyle}" TargetType="{x:Type ann:RulerAnnotation}">
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ann:RulerAnnotation}">
                    <Grid x:Name="PART_AnnotationRoot">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid
                            x:Name="PART_RulerArea"
                            Width="{TemplateBinding ann:RulerAnnotation.RulerWidth}"
                            Height="{TemplateBinding ann:RulerAnnotation.RulerHeight}"
                            HorizontalAlignment="Center"
                            Background="{TemplateBinding Control.Background}"
                            Row="0">
                            <Line
                                VerticalAlignment="Center"
                                Stretch="Fill"
                                Stroke="DimGray"
                                StrokeDashArray="2,3"
                                StrokeThickness="1"
                                X1="0"
                                X2="1"
                                Y1="0"
                                Y2="0" />
                            <Line
                                HorizontalAlignment="Center"
                                Stretch="Fill"
                                Stroke="DimGray"
                                StrokeDashArray="2,3"
                                StrokeThickness="1"
                                X1="0"
                                X2="0"
                                Y1="0"
                                Y2="1" />
                        </Grid>
                        <Border
                            x:Name="PART_TextBorder"
                            Grid.Row="1"
                            Margin="0,5,0,0"
                            HorizontalAlignment="Center"
                            Background="#E05F9EA0"
                            BorderThickness="0"
                            CornerRadius="3"
                            Padding="5,7">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <FrameworkElement.Resources>
                                    <Style TargetType="{x:Type TextBlock}">
                                        <Setter Property="TextBlock.Foreground" Value="White" />
                                        <Setter Property="TextBlock.FontSize" Value="14" />
                                        <Setter Property="TextBlock.FontWeight" Value="SemiBold" />
                                        <Setter Property="FrameworkElement.VerticalAlignment" Value="Center" />
                                    </Style>
                                </FrameworkElement.Resources>
                                <TextBlock Grid.Row="0" Text="{TemplateBinding ann:RulerAnnotation.Text1}" />
                                <TextBlock Grid.Row="1" Text="{TemplateBinding ann:RulerAnnotation.Text2}" />
                            </Grid>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <ControlTemplate x:Key="{x:Static s:LazyControlTemplate.dje_zXWPHZG4CSS2GQXZJSD75W_ejd}" TargetType="{x:Type s:PointMarker}">
        <Image
            Width="{Binding Width}"
            Height="{Binding Height}"
            Source="{Binding MarkerImageSrc, Mode=OneWay}"
            Stretch="Uniform" />
    </ControlTemplate>
</ResourceDictionary>